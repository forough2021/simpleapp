pipeline { 
agent any 
    environment {
        PATH = "/usr/share/maven "
    }
    stages { 
        stage ('Build') { 
            steps { 
              //  echo " clean package"
                sh "mvn clean install"
            }
        }
        stage ('Test') {
            steps {
             //sh "mvn test "
            echo "mvn test"
            }
          
        }  
            
        

    }           
 }
